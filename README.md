# Здорові Хвостики — fix: stock + product code

Ця версія базується на актуальних файлах сайту.

Виправлено:
- сайт читає залишок із поля `products.stock` (те саме поле, яке записує адмінка), з fallback на `stock_quantity`;
- код товару читається з `products.product_code`;
- код відображається на картці товару;
- логіка наявності: 0 = немає, 1–5 = залишилось, 6+ = в наявності;
- кошик обмежує кількість за залишком.

Дизайн та інші секції не перебудовувалися.
