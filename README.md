# Здорові Хвостики — catalog fix

Виправлено запит до Supabase: сайт більше не запитує поле `stock_quantity`, якого немає в поточній схемі, і читає актуальні поля `stock` та `product_code`.

Це було причиною, через яку весь запит `products` падав, а каталог залишався порожнім/показував неправильний статус наявності.

У репозиторії замініть тільки `index.html`, `app.js`, `style.css`, `products.js`. Папку `assets` залиште свою.
